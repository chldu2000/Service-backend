<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.citi.training.groupb.servicedemo.mapper.UserMapper">
    <select id="selectByUserName" parameterType="string" resultType="com.citi.training.groupb.servicedemo.entity.User">
        SELECT * FROM user WHERE user_name = #{userName};
    </select>
</mapper>
